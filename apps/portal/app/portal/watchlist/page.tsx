import { StubView } from '../_views/StubView';

export default function WatchlistPage() {
  return (
    <StubView
      titleKey="watch_title"
      sprint={3}
      description="Watch List — port z prototypu (WatchListView.jsx) přijde s dalším view-port commitem Sprintu 1. CRUD a denní diff worker landují v rámci Sprintu 3 (HANDOFF §8 Týden 3)."
    />
  );
}
