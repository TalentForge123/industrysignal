import { StubView } from '../_views/StubView';

export default function SrscPage() {
  return (
    <StubView
      titleKey="srsc_title"
      sprint={5}
      description="Target Buyer Intelligence Matrix (Supplier Risk Score) — 6-dimenzionální skórování (conc, kapac, fin, geo, alt, score) s ordinálními škálami a Priority tagy. Port prototypu + tabulka srsc_scores + rule engine v packages/srsc/ landují v rámci Sprintu 5 (HANDOFF §22)."
    />
  );
}
