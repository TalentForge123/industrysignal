import { StubView } from '../_views/StubView';

export default function AlertsPage() {
  return (
    <StubView
      titleKey="alerts_title"
      sprint={3}
      description="Alert feed — port z prototypu (AlertsView.jsx) přijde s dalším view-port commitem Sprintu 1. Reálný diff engine + email delivery v rámci Sprintu 3 (HANDOFF §7, §8 Týden 3)."
    />
  );
}
