import { StubView } from '../_views/StubView';

export default function XmapPage() {
  return (
    <StubView
      titleKey="xmap_title"
      sprint={5}
      description="Export Intelligence Map — tepelná mapa CZ→EU dle HS kódů, Top hráči, vstupní okna, kontakty. Port prototypu + reálná data z Celní správy + Eurostat Comext landují v rámci Sprintu 5 (HANDOFF §8 Týden 5)."
    />
  );
}
