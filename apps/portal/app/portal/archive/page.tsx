import { StubView } from '../_views/StubView';

export default function ArchivePage() {
  return (
    <StubView
      titleKey="arch_title"
      sprint={1}
      description="Stažitelné PDF historických kvartálních reportů. Port z prototypu (ArchiveView.jsx) přijde s dalším view-port commitem v rámci Sprintu 1."
    />
  );
}
